const activo = false;

// Evalua si es true o false () ? =true : =false; 
const mensaje = (activo) ? "activo": "inactivo";

// Solo evalua si es true, ejecutara la accion
// Si no cumple, devuelve false
const mensaje1 = activo && "activo"

console.log(mensaje)
console.log(mensaje1)